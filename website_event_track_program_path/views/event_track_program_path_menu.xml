<?xml version="1.0" encoding="utf-8"?>

<odoo>

    <record id="event_track_program_path_action" model="ir.actions.act_window" >
        <field name="name">Program paths</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">event.track.program.path</field>
        <field name="view_type">form</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{}</field>
    </record>

    <menuitem
            id="menu_event_track_program"
            name="Program paths"
            parent="event.event_main_menu"
            sequence="15"
            groups="event.group_event_manager"/>

    <menuitem id="event_track_program_path_menu"
              name="Program paths"
              action="event_track_program_path_action"
              sequence="10"
              parent="website_event_track_program_path.menu_event_track_program"
              groups="event.group_event_manager"
              />

</odoo>