<?xml version="1.0" encoding="UTF-8"?>
<odoo noupdate="1">

    <record id="email_template_event_track_announced" model="mail.template">
        <field name="name">Event track mail - announced</field>
        <field name="model_id" ref="website_event_track.model_event_track"/>
        <field name="auto_delete" eval="True"/>
        <field name="lang">${object.partner_id.lang}</field>
        <field name="email_from">${(object.event_id.organizer_id.email or object.event_id.user_id.email or '')|safe}</field>
        <field name="partner_to" >${object.partner_id.id}</field>
        <field name="subject">${object.event_id.name} - your presentation was selected to the programme</field>
        <field name="body_html"><![CDATA[
% set website_url = object.env['ir.config_parameter'].sudo().get_param('web.base.url')
<p>Dear ${object.partner_id.name},</p>
<p>Thank you for your proposal <strong>${object.name}</strong>.</p>

<p>Your proposal has been <strong>approved</strong> to the ${object.event_id.name} programme. Congratulations!</p>
<p>The proposal had an average rating of <strong>${object.rating_avg}</strong> with scale of 1-5.</p>

<p>Proposal contact, please take care of these:</p>
<ul>
    <li>Check and edit your presentation contents. The final contents will be used in conference handbill and publications.</li>
    <li>Confirm the presentation after you have done all the necessary edits.</li>
    <li>Unconfirmed presentations will be left out from conference programme.</li>
</ul>

% if 'website_url':
<p>The proposal can be edited in <a href="${website_url}/my/tracks" style="color:#875A7B;">My proposals</a></p>
% endif

<p>Welcome to the conference!</p>
]]></field>
    </record>
</odoo>
