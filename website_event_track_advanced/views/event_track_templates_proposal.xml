<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <template
        id="event_track_proposal_advanced"
        inherit_id="website_event_track.event_track_proposal"
    >
        <xpath expr="//section[@class='pt48']//h2" position="attributes">
            <attribute
                name="class"
            >text-center text-primary font-weight-bold my-4</attribute>
        </xpath>
        <xpath expr="//div[@t-if='not track']" position="replace">
            <div t-if="not submitted">
                <div
                    t-if="event.website_track_proposal or request.env.user.has_group('event.group_event_manager')"
                >
                    <section class="mt-5">
                        <t t-if="tracks">
                            <h3 class="o_page_header">Applications</h3>
                            <p>
                            Manage existing proposals or propose a new talk
                            </p>
                        </t>
                        <t t-else="">
                            <h3 class="o_page_header">Application</h3>
                            <p>
                                Fill this form to propose your talk.
                            </p>
                        </t>
                    </section>
                    <div class="row">
                        <t t-foreach="tracks" t-as="track">
                            <t
                                t-call="website_event_track_advanced.event_track_proposal_template"
                            />
                        </t>
                    </div>
                    <t t-set="track" t-value="request.env['event.track']" />
                    <t
                        t-call="website_event_track_advanced.event_track_proposal_template_new"
                    />
                </div>
            </div>
        </xpath>
    </template>
    <template
        id="event_track_proposal_success"
        inherit_id="website_event_track.event_track_proposal_success"
    >
        <xpath expr="//section//p[last()]" position="replace">
            <t t-if="track.stage_id.is_editable and track.stage_id.is_draft">
                <p>
                    Your proposal is saved as a draft and will not be reviewed until it is submitted.
                </p>
                <t t-if="not user_exists">
                    <p>
                        An account has been created for the username <b><t
                                t-esc="track.partner_id.user_ids[0].login"
                            /></b>. You should receive an email shortly to finish seting up your account. After setting up your account, you can go to <a
                            href="/my/tracks"
                        >My Tracks</a> to edit and submit your proposal.
                    </p>
                </t>
                <t t-else="">
                    <p>
                        An account for the username <b><t
                                t-esc="track.partner_id.user_ids[0].login"
                            /></b> exists in the system. After logging into your account, you can go to <a
                            href="/my/tracks"
                        >My Tracks</a> to edit and submit your proposal. If you have forgotten your password, please click <a
                            href="/web/reset_password"
                        >here</a>.
                    </p>
                </t>
            </t>
            <t t-elif="track.stage_id.is_done">
                <p>
                    Your track is saved and confirmed as part of the event.
                </p>
                <t t-if="not user_exists">
                    <p>
                        An account has been created for the username <b><t
                                t-esc="track.partner_id.user_ids[0].login"
                            /></b>. You should receive an email shortly to finish seting up your account. After setting up your account, you can go to <a
                            href="/my/tracks"
                        >My Tracks</a> to view your track.
                    </p>
                </t>
                <t t-else="">
                    <p>
                        An account for the username <b><t
                                t-esc="track.partner_id.user_ids[0].login"
                            /></b> exists in the system. After logging into your account, you can go to <a
                            href="/my/tracks"
                        >My Tracks</a> to view your track. If you have forgotten your password, please click <a
                            href="/web/reset_password"
                        >here</a>.
                    </p>
                </t>
            </t>
            <t t-else="">
                <p>
                    We will evaluate your proposition and get back to you shortly.
                </p>
                <t t-if="not user_exists">
                    <p>
                        An account has been created for the username <b><t
                                t-esc="track.partner_id.user_ids[0].login"
                            /></b>. You should receive an email shortly to finish seting up your account. After setting up your account, you can go to <a
                            href="/my/tracks"
                        >My Tracks</a> to view your proposal.
                    </p>
                </t>
                <t t-else="">
                    <p>
                        An account for the username <b><t
                                t-esc="track.partner_id.user_ids[0].login"
                            /></b> exists in the system. After logging into your account, you can go to <a
                            href="/my/tracks"
                        >My Tracks</a> to view your proposal. If you have forgotten your password, please click <a
                            href="/web/reset_password"
                        >here</a>.
                    </p>
                </t>
            </t>
        </xpath>
    </template>
    <template id="event_track_proposal_template_new" name="Event Track New Application">
        <div>
            <form
                id="track_proposal"
                class="mb-5"
                t-attf-action="/event/#{slug(event)}/track_proposal/form"
                method="post"
                itemscope="itemscope"
                itemprop="offers"
                itemtype="http://schema.org/AggregateOffer"
            >
                <input
                    type="hidden"
                    name="csrf_token"
                    t-att-value="request.csrf_token()"
                />
                <input type="hidden" name="track_id" t-att-value="track.id" />
                <button
                    type="submit"
                    class="btn btn-primary o_wait_lazy_js btn-block a-submit"
                    t-attf-id="#{track.id}"
                >New Proposal</button>
            </form>
        </div>
    </template>
    <template id="event_track_proposal_template" name="Event Track Application">
        <div class="col-xl-6">
            <form
                id="track_proposal"
                class="mb-5"
                t-attf-action="/event/#{slug(event)}/track_proposal/form"
                method="post"
                itemscope="itemscope"
                itemprop="offers"
                itemtype="http://schema.org/AggregateOffer"
            >
                <input
                    type="hidden"
                    name="csrf_token"
                    t-att-value="request.csrf_token()"
                />
                <input type="hidden" name="track_id" t-att-value="track.id" />
                <div t-attf-class="card border border-primary">
                    <h4 class="card-header">
                        <t t-esc="track.name" />
                        <td class="td-submit">
                            <t t-set="track_state">badge-default</t>
                            <t
                                t-if="track.stage_id.is_editable and track.stage_id.is_draft"
                            >
                                <t t-set="track_state">badge-info</t>
                            </t>
                            <t t-if="track.stage_id.is_submitted">
                                <t t-set="track_state">badge-warning</t>
                            </t>
                            <t t-if="track.stage_id.is_accepted">
                                <t t-set="track_state">badge-primary</t>
                            </t>
                            <t t-if="track.stage_id.is_done">
                                <t t-set="track_state">badge-success</t>
                            </t>
                            <t t-if="track.stage_id.is_cancel">
                                <t t-set="track_state">badge-danger</t>
                            </t>
                            <span
                                t-field="track.stage_id"
                                t-attf-class="badge {{track_state}}"
                            />
                        </td>
                        <button
                            type="submit"
                            class="btn btn-link a-submit float-right p-0 no-decoration o_wait_lazy_js"
                            role="button"
                            title="Edit this proposal"
                            aria-label="Edit this proposal"
                            t-attf-id="#{track.id}"
                        >
                            <i class="fa fa-edit" />
                        </button>
                    </h4>
                    <div class="card-body">
                        <div
                            t-field="track.partner_id"
                            t-options='{"widget": "contact", "fields": ["email", "phone", "address", "name"]}'
                        />
                    </div>
                    <div class="card-footer">
                        <button
                            type="submit"
                            class="btn btn-link a-submit p-0 no-decoration o_wait_lazy_js"
                            role="button"
                            title="Edit this proposal"
                            aria-label="Edit this proposal"
                            t-attf-id="#{track.id}"
                        >Edit this proposal</button>
                    </div>
                </div>
            </form>
        </div>
    </template>
    <!-- Warning modal -->
    <template name="Event Track Warning" id="event_track_warning">
        <div
            t-att-id="modal_id if modal_id else 'modal_event_track_warning'"
            class="modal fade"
            tabindex="-1"
            role="dialog"
            data-backdrop="false"
        >
            <div class="modal-dialog" role="document">
                <div class="modal-content">
                    <div class="modal-header align-items-center">
                        <h4 class="modal-title"><t t-esc="modal_title" /></h4>
                        <button
                            type="button"
                            t-attf-class="close warning-cancel-#{modal_id}"
                            aria-label="Close"
                        >
                            <span>×</span>
                        </button>
                    </div>
                    <div class="modal-body">
                        <strong><t t-esc="modal_text" /></strong>
                    </div>
                    <div class="modal-footer border-0 justify-content-between">
                        <button
                            type="button"
                            t-attf-class="btn btn-secondary warning-cancel-#{modal_id}"
                        >Cancel</button>
                        <button
                            type="button"
                            name="warning-confirm"
                            t-attf-class="btn btn-primary warning-confirm-#{modal_id}"
                        >Confirm</button>
                    </div>
                </div>
            </div>
        </div>
    </template>
    <!-- Close modal warning -->
    <template
        name="Event Track Warning Close Modal"
        id="event_track_warning_close_modal"
    >
        <t t-call="website_event_track_advanced.event_track_warning">
            <t t-set="modal_id">modal_event_track_warning_close_modal</t>
            <t t-set="modal_title">Close Proposal</t>
            <t
                t-set="modal_text"
            >Unsaved changes will be lost if you leave the page, are you sure?</t>
        </t>
    </template>
    <template
        name="Event Track Warning Remove Presenter"
        id="event_track_warning_remove_presenter"
    >
        <t t-call="website_event_track_advanced.event_track_warning">
            <t t-set="modal_id">modal_event_track_warning_remove_presenter</t>
            <t t-set="modal_title">Remove Presenter</t>
            <t t-set="modal_text">Remove this presenter, are you sure?</t>
        </t>
    </template>
    <!-- Application -->
    <template name="Event Track Application" id="event_track_application">
        <div
            id="modal_event_track_application"
            class="modal fade"
            tabindex="-1"
            role="dialog"
        >
            <div class="modal-dialog modal-lg" role="document">
                <form
                    id="track-application-form"
                    class="mt32 js_website_submit_form"
                    t-attf-action="/event/#{event.id}/track_proposal/post"
                    method="post"
                    enctype="multipart/form-data"
                >
                    <input
                        type="hidden"
                        name="csrf_token"
                        t-att-value="request.csrf_token(time_limit=False)"
                    />
                    <input
                        type="hidden"
                        id="editable"
                        name="editable"
                        t-att-value="editable"
                    />
                    <input type="hidden" id="draft" name="draft" t-att-value="draft" />
                    <input type="hidden" name="track_id" t-att-value="track.id" />
                    <div class="modal-content">
                        <div class="modal-header align-items-center">
                            <t t-if="not review">
                                <h4 class="modal-title">Proposal</h4>
                            </t>
                            <t t-else="">
                                <h4 class="modal-title">Rate Proposal</h4>
                            </t>
                            <button
                                type="button"
                                class="close warning-close-modal"
                                aria-label="Close"
                            >
                                <span>×</span>
                            </button>
                        </div>
                        <div class="modal-body bg-light border-bottom">
                            <t
                                t-call="website_event_track_advanced.event_track_application_type"
                            />
                            <t
                                t-call="website_event_track_advanced.event_track_application_info"
                            />
                            <t
                                t-call="website_event_track_advanced.event_track_application_targetgroup_info"
                            />
                            <t t-if="not review">
                                <t
                                    t-call="website_event_track_advanced.event_track_application_contact_info"
                                />
                                <t
                                    t-call="website_event_track_advanced.event_track_application_speakers"
                                />
                                <t
                                    t-call="website_event_track_advanced.event_track_application_webinar"
                                />
                                <t
                                    t-call="website_event_track_advanced.event_track_application_workshop"
                                />
                                <t
                                    t-call="website_event_track_advanced.event_track_application_extra_info"
                                />
                            </t>
                            <t t-if="review">
                                <t
                                    t-call="website_event_track_advanced.event_track_application_review"
                                />
                            </t>
                        </div>
                        <t t-if="not review">
                            <div class="modal-body bg-light border-bottom">
                                <p
                                    class="alert alert-info alert-dismissable"
                                    t-if="editable and draft"
                                >
                                    <a
                                        href="#"
                                        class="close"
                                        data-dismiss="alert"
                                        aria-label="close"
                                    >×</a>
                                    <i class="fa fa-info-circle" />
                                    <span>By clicking <strong
                                        >"Save as Draft"</strong> the proposal shall be saved and can be edited later by the contact person.</span>
                                </p>
                                <p
                                    class="alert alert-info alert-dismissable"
                                    t-if="editable and draft"
                                >
                                    <a
                                        href="#"
                                        class="close"
                                        data-dismiss="alert"
                                        aria-label="close"
                                    >×</a>
                                    <i class="fa fa-info-circle" />
                                    <span>By clicking <strong
                                        >"Save and Submit"</strong> the proposal shall be saved and submitted for review.</span>
                                    <br />
                                    <span
                                    >Please note that a completed proposal can no longer be edited.</span>
                                </p>
                                <p
                                    class="alert alert-info alert-dismissable"
                                    t-if="editable and not draft"
                                >
                                    <a
                                        href="#"
                                        class="close"
                                        data-dismiss="alert"
                                        aria-label="close"
                                    >×</a>
                                    <i class="fa fa-info-circle" />
                                    <span>By clicking <strong
                                        >"Save and Confirm"</strong> the changes shall be saved and the track will be confirmed as part of the event.</span>
                                    <br />
                                    <span
                                    >Please note that a confirmed track can no longer be edited.</span>
                                </p>
                            </div>
                            <div class="modal-footer border-0 justify-content-between">
                                <button
                                    type="button"
                                    class="btn btn-secondary warning-close-modal"
                                >Cancel</button>
                                <button
                                    type="submit"
                                    name="track-draft"
                                    value="track-draft"
                                    id="application-submit-button"
                                    class="btn btn-primary application-submit"
                                    data-toggle="tooltip"
                                    data-position="top"
                                    t-att-disabled="None if editable and draft else 'disabled'"
                                    title="By clicking &quot;Save as Draft&quot; the proposal shall be saved and can be edited later by the contact person."
                                >Save as Draft</button>
                                <t t-if="not draft">
                                    <button
                                        type="submit"
                                        name="track-is-done"
                                        value="track-is-done"
                                        id="application-submit-button-send"
                                        class="btn btn-primary application-submit"
                                        data-toggle="tooltip"
                                        data-position="top"
                                        t-att-disabled="None if editable else 'disabled'"
                                        title="By clicking &quot;Save and Confirm&quot; the changes shall be saved and the track will be confirmed as part of the event. Please note that a confirmed track can no longer be edited."
                                    >Save and Confirm</button>
                                </t>
                                <t t-else="">
                                    <button
                                        type="submit"
                                        name="track-confirm"
                                        value="track-confirm"
                                        id="application-submit-button-send"
                                        class="btn btn-primary application-submit"
                                        data-toggle="tooltip"
                                        data-position="top"
                                        t-att-disabled="None if editable else 'disabled'"
                                        title="By clicking &quot;Save and Submit&quot; the proposal shall be saved and submitted for review. Please note that a completed proposal can no longer be edited."
                                    >Save and Submit</button>
                                </t>
                            </div>
                        </t>
                        <t t-else="">
                            <div class="modal-body bg-light border-bottom">
                                <p class="alert alert-info alert-dismissable">
                                    <a
                                        href="#"
                                        class="close"
                                        data-dismiss="alert"
                                        aria-label="close"
                                    >×</a>
                                    <i class="fa fa-info-circle" />
                                    <span>By clicking <strong
                                        >"Submit Review"</strong> the review of the proposal shall be submitted.</span>
                                </p>
                            </div>
                            <div class="modal-footer border-0 justify-content-between">
                                <button
                                    type="button"
                                    class="btn btn-secondary warning-close-modal"
                                >Cancel</button>
                                <button
                                    type="submit"
                                    name="review-confirm"
                                    value="review-confirm"
                                    id="application-submit-button-send"
                                    class="btn btn-primary application-submit"
                                    data-toggle="tooltip"
                                    data-position="top"
                                    title="By clicking &quot;Submit Review&quot; the review of the proposal shall be submitted."
                                >Submit Review</button>
                            </div>

                        </t>
                    </div>
                </form>
            </div>
            <t t-call="website_event_track_advanced.event_track_warning_close_modal" />
            <t
                t-call="website_event_track_advanced.event_track_warning_remove_presenter"
            />
        </div>
    </template>

    <!-- REVIEW -->
    <template name="Event Track Review" id="event_track_review">
        <div
            id="modal_event_track_application"
            class="modal fade"
            tabindex="-1"
            role="dialog"
        >
            <div class="modal-dialog modal-lg" role="document">
                <form
                    id="track-application-form"
                    class="mt32 js_website_submit_form"
                    t-attf-action="/event/#{event.id}/track_proposal/review/post"
                    method="post"
                    enctype="multipart/form-data"
                >
                    <input
                        type="hidden"
                        name="csrf_token"
                        t-att-value="request.csrf_token(time_limit=False)"
                    />
                    <input
                        type="hidden"
                        id="editable"
                        name="editable"
                        t-att-value="editable"
                    />
                    <input type="hidden" name="track_id" t-att-value="track.id" />
                    <div class="modal-content">
                        <div class="modal-header align-items-center">
                            <t t-if="editable">
                            <h4 class="modal-title">Review Proposal</h4>
                            </t>
                            <button
                                type="button"
                                class="close warning-close-modal"
                                aria-label="Close"
                            >
                                <span>×</span>
                            </button>
                        </div>
                        <div class="modal-body bg-light border-bottom">
                            <t
                                t-call="website_event_track_advanced.event_track_application_type"
                            />
                            <t
                                t-call="website_event_track_advanced.event_track_application_info"
                            />
                            <t
                                t-call="website_event_track_advanced.event_track_application_targetgroup_info"
                            />
                        </div>
                        <div class="modal-footer border-0 justify-content-between">
                            <button
                                type="button"
                                class="btn btn-secondary warning-close-modal"
                            >Cancel</button>
                            <button
                                type="submit"
                                name="review-confirm"
                                value="review-confirm"
                                id="review-submit-button-send"
                                class="btn btn-primary review-submit"
                                data-toggle="tooltip"
                                data-position="top"
                                title="By clicking &quot;Submit Review&quot; you will submit your review of the proposal."
                            >Submit Review</button>
                        </div>
                    </div>
                </form>
            </div>
            <t t-call="website_event_track_advanced.event_track_warning_close_modal" />
        </div>
    </template>

    <!-- Application type -->
    <template id="event_track_application_type">
        <div class="panel panel-primary" id="track-application-type-div">
            <div class="panel-heading">
                <h3 class="panel-title">Application Type</h3>
            </div>
            <div class="panel-body">
                <div class="row" id="track-application-application-type-div">
                    <div class="form-group col-xl-6">
                        <label
                            class="col-form-label"
                            for="type"
                        >Application Type</label>
                        <select
                            name="type"
                            id="type"
                            class="form-control"
                            t-att-required="None if not editable else 'required'"
                            t-att-disabled="None if editable else 'disabled'"
                        >
                            <option value="">Select...</option>
                            <t
                                t-foreach="event.track_types_ids or []"
                                t-as="application_type"
                            >
                                <option
                                    t-att-value="application_type.id"
                                    t-att-selected="application_type.id == track.type.id"
                                    t-att-data-workshop="application_type.workshop"
                                    t-att-data-workshop-contract="application_type.workshop_contract"
                                    t-att-data-webinar="application_type.webinar"
                                    t-att-data-description="application_type.description"
                                >
                                    <t t-esc="application_type.name" />
                                </option>
                            </t>
                        </select>
                        <div
                            class="invalid-feedback"
                        >Please choose an application type.</div>
                    </div>
                    <div class="form-group col-xl-6">
                        <label
                            for="type_description"
                            class="control-label"
                        >Description</label>
                        <p id="application_type_description" name="type_description">
                            <t t-esc="track.type.description" />
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </template>
    <!-- Application info -->
    <template id="event_track_application_info">
        <div class="panel panel-primary" id="track-application-application-div">
            <div class="panel-heading">
                <h3 class="panel-title">Application Info</h3>
            </div>
            <div class="panel-body">
                <p
                    t-if="not review"
                    id="track-application-application-alert-info"
                    class="alert alert-info alert-dismissable"
                ><a href="#" class="close" data-dismiss="alert" aria-label="close">×</a>
                    This part of the application is disclosed to the reviewers.
                </p>
                <p
                    t-if="track.stage_id.is_editable and track.stage_id.is_draft and not review"
                    id="track-application-application-alert-warning"
                    class="alert alert-warning alert-dismissable"
                ><a href="#" class="close" data-dismiss="alert" aria-label="close">×</a>
                    Avoid using identifiable information (name, organisation, project).
                </p>
                <div class="row" id="track-application-application-info-div">
                    <div class="form-group col-xl-6">
                        <label
                            for="name"
                            class="col-form-label"
                        >Presentation Title</label>
                        <input
                            name="name"
                            maxlength="100"
                            placeholder="Presentation Title"
                            t-att-value="track.name"
                            t-att-required="None if not editable else 'required'"
                            class="form-control"
                            type="text"
                            t-att-disabled="None if editable else 'disabled'"
                        />
                        <div
                            class="invalid-feedback"
                        >Please enter a presentation title.</div>
                    </div>
                    <div class="form-group col-xl-6">
                        <label
                            for="video_url"
                            class="col-form-label label-optional"
                        >Presentation in video format (link to e.g. YouTube)</label>
                        <input
                            name="video_url"
                            placeholder="https://www.youtube.com/..."
                            t-att-value="track.video_url"
                            class="form-control"
                            type="text"
                            t-att-disabled="None if editable else 'disabled'"
                        />
                    </div>
                    <div class="form-group col-xl-12">
                        <label
                            for="description"
                            class="col-form-label"
                        >Content of the Presentation</label>
                        <p
                            t-if="not review"
                            id="track-application-description-alert-warning"
                            class="alert alert-warning alert-dismissable"
                        >
                            <a
                                href="#"
                                class="close"
                                data-dismiss="alert"
                                aria-label="close"
                            >×</a>
                            <span>Max. 500 words.</span>
                        </p>
                        <textarea
                            type="text"
                            name="description"
                            placeholder="Application Content..."
                            class="form-control o_wysiwyg_loader"
                            t-att-required="None if not editable else 'required'"
                            t-att-disabled="None if editable else 'disabled'"
                            rows="6"
                        ><t t-esc="track.description" /></textarea>
                        <div
                            class="invalid-feedback"
                        >Please enter presentation content.</div>
                    </div>
                    <div class="form-group col-xl-6">
                        <label
                            class="col-form-label"
                            for="language"
                        >Language of the Presentation</label>
                        <select
                            name="language"
                            t-attf-class="form-control"
                            t-att-required="None if not editable else 'required'"
                            t-att-disabled="None if editable else 'disabled'"
                        >
                            <option value="">Select...</option>
                            <t t-foreach="track_languages or []" t-as="language">
                                <option
                                    t-att-value="language.id"
                                    t-att-selected="language.id == track.language.id"
                                >
                                    <t t-esc="language.name" />
                                </option>
                            </t>
                        </select>
                        <div
                            class="invalid-feedback"
                        >Please select a presentation language.</div>
                    </div>
                    <div t-if="not review" class="form-group col-xl-6">
                        <label
                            class="col-form-label label-optional"
                            for="attachment_ids"
                        >Add Attachments (optional)</label>
                        <button
                            type="button"
                            id="btn-remove-attachment"
                            class="btn btn-default"
                            data-toggle="tooltip"
                            data-placement="right"
                            title="Remove Attachments"
                            t-att-disabled="None if editable else 'disabled'"
                        >
                            <i class="fa fa-trash" />
                        </button>
                        <input
                            type="file"
                            id="attachment_ids"
                            name="attachment_ids"
                            t-att-value="track.attachment_ids"
                            class="form-control-file"
                            multiple="true"
                            t-att-disabled="None if editable else 'disabled'"
                        />
                        <div
                            t-if="track.attachment_ids"
                            class="o_track_proposal_attachment_container pt-3"
                        >
                            <span
                                class="text-muted"
                            >Previously uploaded attachments:</span>
                            <t t-foreach="track.attachment_ids" t-as="attachment">
                                <div
                                    class="o_track_proposal_attachment"
                                    t-att-id="attachment.id"
                                >
                                    <a
                                        t-att-href="attachment.link_url"
                                        aria-label="Download"
                                        title="Download"
                                        target="_blank"
                                    >
                                        <i class="fa fa-download" />
                                        <span>
                                            <t t-esc="attachment.name" />
                                        </span>
                                    </a>
                                </div>
                            </t>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </template>
    <!-- Target group info -->
    <template id="event_track_application_targetgroup_info">
        <div
            class="panel panel-primary"
            id="track-application-targetgroup-div"
            t-if="event.sudo().target_group_ids"
        >
            <div class="panel-heading">
                <h3 class="panel-title">Target Group Details</h3>
            </div>
            <div class="panel-body">
                <p
                    t-if="not review"
                    id="track-application-targetgroup-alert-info"
                    class="alert alert-info alert-dismissable"
                >
                    <a
                        href="#"
                        class="close"
                        data-dismiss="alert"
                        aria-label="close"
                    >×</a>
                    <span>Select your presentation’s primary target group.</span>
                    <br />
                    <span
                    >You can enter specifications of your presentation’s target group in the target group details.</span>
                </p>
                <div class="row" id="track-application-targetgroup-info-div">
                    <div class="form-group col-xl-12">
                        <label
                            for="target_groups"
                            class="col-form-label label-optional"
                        >Target group(s)</label>
                        <p
                            t-if="not review and event.allow_target_group_multiple"
                            id="track-application-tag-alert-info"
                            class="alert alert-info alert-dismissable"
                        >
                            <a
                                href="#"
                                class="close"
                                data-dismiss="alert"
                                aria-label="close"
                            >×</a>
                            <span>Please select up to three target groups.</span>
                        </p>
                        <t t-if="event.allow_target_group_multiple">
                            <select
                                name="target_groups"
                                id="target_groups"
                                placeholder="Target groups..."
                                multiple="multiple"
                                class="form-control target-groups-select"
                                autocomplete="off"
                                t-att-disabled="None if editable else 'disabled'"
                            >
                                <t
                                    t-foreach="event.sudo().target_group_ids or []"
                                    t-as="t"
                                >
                                    <option
                                        t-att-value="t.id"
                                        t-att-selected="t.id in track.target_group_ids.ids"
                                    ><t t-esc="t.name" /></option>
                                </t>
                            </select>
                        </t>
                        <t t-else="">
                            <select
                                name="target_groups"
                                id="target_groups"
                                placeholder="Target group..."
                                class="form-control"
                                autocomplete="off"
                                t-att-disabled="None if editable else 'disabled'"
                            >
                                <option value="">Select...</option>
                                <t t-foreach="event.target_group_ids or []" t-as="t">
                                    <option
                                        t-att-value="t.id"
                                        t-att-selected="t.id in track.target_group_ids.ids"
                                    ><t t-esc="t.name" /></option>
                                </t>
                            </select>
                        </t>
                        <div
                            class="invalid-feedback"
                        >Please select a presentation target group.</div>
                    </div>
<!--                     <div class="form-group col-xl-12">
                        <label
                            class="col-form-label"
                            for="target_group"
                        >Target Group</label>
                        <select
                            name="target_group"
                            class="form-control"
                            t-att-required="None if not editable else 'required'"
                            t-att-disabled="None if editable else 'disabled'"
                        >
                            <option value="">Select...</option>
                            <t
                                t-foreach="event.sudo().target_group_ids or []"
                                t-as="target_group"
                            >
                                <option
                                    t-att-value="target_group.id"
                                    t-att-selected="target_group.id == track.target_group.id"
                                >
                                    <t t-esc="target_group.name" />
                                </option>
                            </t>
                        </select>

                        <div
                            class="invalid-feedback"
                        >Please select a presentation target group.</div>
                    </div> -->
                    <div class="form-group col-xl-12">
                        <label
                            for="target_group_info"
                            class="col-form-label label-optional"
                        >Target Group Details</label>
                        <br />
                        <textarea
                            type="text"
                            name="target_group_info"
                            placeholder="Target Group Info..."
                            class="form-control o_wysiwyg_loader"
                            t-att-disabled="None if editable else 'disabled'"
                            rows="6"
                        ><t t-esc="track.target_group_info" /></textarea>
                    </div>
                    <div class="form-group col-xl-12">
                        <label
                            for="tags"
                            class="col-form-label label-optional"
                        >Tags</label>
                        <p
                            t-if="not review and event.allow_track_tags_multiple"
                            id="track-application-tag-alert-info"
                            class="alert alert-info alert-dismissable"
                        >
                            <a
                                href="#"
                                class="close"
                                data-dismiss="alert"
                                aria-label="close"
                            >×</a>
                            <span>Please select up to three tags.</span>
                        </p>
                        <t t-if="event.allow_track_tags_multiple">
                            <select
                                name="tags"
                                id="tags"
                                placeholder="Tags..."
                                multiple="multiple"
                                class="form-control tags-select"
                                autocomplete="off"
                                t-att-disabled="None if editable else 'disabled'"
                            >
                                <t
                                    t-foreach="event.allowed_track_tag_ids or []"
                                    t-as="tag"
                                >
                                    <option
                                        t-att-value="tag.id"
                                        t-att-selected="tag.id in track.tag_ids.ids"
                                    ><t t-esc="tag.name" /></option>
                                </t>
                            </select>
                        </t>
                        <t t-else="">
                            <select
                                name="tags"
                                id="tags"
                                placeholder="Tag..."
                                class="form-control"
                                autocomplete="off"
                                t-att-disabled="None if editable else 'disabled'"
                            >
                                <option value="">Select...</option>
                                <t
                                    t-foreach="event.allowed_track_tag_ids or []"
                                    t-as="tag"
                                >
                                    <option
                                        t-att-value="tag.id"
                                        t-att-selected="tag.id in track.tag_ids.ids"
                                    ><t t-esc="tag.name" /></option>
                                </t>
                            </select>
                        </t>
                    </div>
                </div>
            </div>
        </div>
        <div class="panel panel-primary" t-else="">
            <div class="panel-body">
                <div class="row" id="track-application-targetgroup-info-div">
                    <div class="form-group col-xl-12">
                        <label
                            for="tags"
                            class="col-form-label label-optional"
                        >Tags</label>
                        <p
                            t-if="not review and event.allow_track_tags_multiple"
                            id="track-application-tag-alert-info"
                            class="alert alert-info alert-dismissable"
                        >
                            <a
                                href="#"
                                class="close"
                                data-dismiss="alert"
                                aria-label="close"
                            >×</a>
                            <span>Please select up to three tags.</span>
                        </p>
                        <t t-if="event.allow_track_tags_multiple">
                            <select
                                name="tags"
                                id="tags"
                                placeholder="Tags..."
                                multiple="multiple"
                                class="form-control tags-select"
                                autocomplete="off"
                                t-att-disabled="None if editable else 'disabled'"
                            >
                                <t
                                    t-foreach="event.allowed_track_tag_ids or []"
                                    t-as="tag"
                                >
                                    <option
                                        t-att-value="tag.id"
                                        t-att-selected="tag.id in track.tag_ids.ids"
                                    ><t t-esc="tag.name" /></option>
                                </t>
                            </select>
                        </t>
                        <t t-else="">
                            <select
                                name="tags"
                                id="tags"
                                placeholder="Tag..."
                                class="form-control"
                                autocomplete="off"
                                t-att-disabled="None if editable else 'disabled'"
                            >
                                <option value="">Select...</option>
                                <t
                                    t-foreach="event.allowed_track_tag_ids or []"
                                    t-as="tag"
                                >
                                    <option
                                        t-att-value="tag.id"
                                        t-att-selected="tag.id in track.tag_ids.ids"
                                    ><t t-esc="tag.name" /></option>
                                </t>
                            </select>
                        </t>
                    </div>
                </div>
            </div>
        </div>
    </template>
    <!-- Contact info -->
    <template id="website_event_track_advanced.event_track_application_contact_info">
        <div class="panel panel-primary" id="track-application-contact-div">
            <div class="panel-heading">
                <h3 class="panel-title">Presentation’s Contact Person</h3>
            </div>
            <div class="panel-body">
                <p
                    id="track-application-contact-alert-info"
                    class="alert alert-info alert-dismissable"
                ><a href="#" class="close" data-dismiss="alert" aria-label="close">×</a>
                    Please fill the contact persons information here.
                </p>
                <p
                    id="track-application-contact-alert-warning"
                    class="alert alert-warning alert-dismissable"
                ><a href="#" class="close" data-dismiss="alert" aria-label="close">×</a>
                    An account for the contact person will be created upon form submission.
                </p>
                <div id="track-application-contact-row-div" class="row">
                    <div class="form-group col-xl-4">
                        <input
                            t-attf-name="contact_id"
                            t-att-value="track.partner_id.id"
                            class="d-none"
                            type="hidden"
                        />
                        <label
                            for="contact_firstname"
                            class="col-form-label"
                        >First Name</label>
                        <input
                            name="contact_firstname"
                            placeholder="First Name"
                            t-att-value="track.partner_id.firstname or partner.firstname if partner else False"
                            class="form-control"
                            type="text"
                            t-att-required="None if not editable else 'required'"
                            t-att-disabled="None if editable else 'disabled'"
                        />
                    </div>
                    <div class="form-group col-xl-4">
                        <label
                            for="contact_lastname"
                            class="col-form-label"
                        >Last Name</label>
                        <input
                            name="contact_lastname"
                            placeholder="Last Name"
                            t-att-value="track.partner_id.lastname or partner.lastname if partner else False"
                            class="form-control"
                            type="text"
                            t-att-required="None if not editable else 'required'"
                            t-att-disabled="None if editable else 'disabled'"
                        />
                    </div>
                    <div class="form-group col-xl-4">
                        <label for="contact_email" class="col-form-label">Email</label>
                        <input
                            name="contact_email"
                            placeholder="Email"
                            t-att-value="track.partner_id.email or partner.email if partner else False"
                            class="form-control"
                            type="email"
                            t-att-required="None if not editable else 'required'"
                            t-att-disabled="None if editable else 'disabled'"
                            t-att-readonly="None if not track.partner_id.email else 'readonly'"
                        />
                    </div>
                    <div class="form-group col-xl-4">
                        <label
                            for="contact_phone"
                            class="col-form-label label-optional"
                        >Phone</label>
                        <input
                            name="contact_phone"
                            placeholder="Phone"
                            t-att-value="track.partner_id.phone or partner.phone if partner else False"
                            class="form-control"
                            type="tel"
                            t-att-disabled="None if editable else 'disabled'"
                        />
                    </div>
                    <div class="form-group col-xl-4">
                        <label
                            for="contact_organization"
                            class="col-form-label"
                        >Organization</label>
                        <input
                            name="contact_organization"
                            placeholder="Organization"
                            t-att-value="track.partner_id.parent_id.name or partner.parent_id.name if partner else False"
                            class="form-control"
                            type="text"
                            t-att-required="None if not editable else 'required'"
                            t-att-disabled="None if editable else 'disabled'"
                        />
                    </div>
                    <div class="form-group col-xl-4">
                        <label for="contact_title" class="col-form-label">Title</label>
                        <input
                            name="contact_title"
                            placeholder="Title"
                            t-att-value="track.partner_id.function or partner.function if partner else False"
                            class="form-control"
                            type="text"
                            t-att-required="None if not editable else 'required'"
                            t-att-disabled="None if editable else 'disabled'"
                        />
                    </div>
                </div>
            </div>
        </div>
    </template>
    <template id="website_event_track_advanced.event_track_application_speakers">
        <div class="panel panel-primary" id="track-application-speakers-div">
            <div class="panel-heading">
                <h3 class="panel-title">Presenters</h3>
            </div>
            <div class="panel-body">
                <p
                    id="track-application-speakers-alert-info"
                    class="alert alert-info alert-dismissable"
                >
                    <a
                        href="#"
                        class="close"
                        data-dismiss="alert"
                        aria-label="close"
                    >×</a>
                    <span
                        class="alert-message"
                    >List the details of all presenters</span>
                </p>
                <t t-set="speaker_count" t-value="0" />
                <div class="track-application-speakers-div-container">
                    <!-- Add existing speakers to form -->
                    <t t-foreach="track.speaker_ids" t-as="speaker">
                        <t t-set="speaker_count" t-value="speaker_count + 1" />
                        <input
                            t-attf-name="speaker_id[#{speaker_count}]"
                            t-att-value="speaker.id"
                            class="d-none"
                            type="hidden"
                        />
                        <div
                            class="track-application-speakers-div-row-container"
                            t-att-id="speaker_count"
                        >
                            <span class="text-muted presenter-span">Presenter #<t
                                    t-esc="speaker_count"
                                /></span>
                            <button
                                type="button"
                                class="btn btn-default btn-remove-speaker"
                                data-toggle="tooltip"
                                data-placement="right"
                                title="Remove speaker"
                                t-att-disabled="None if editable and speaker_count &gt; 1 else 'disabled'"
                            >
                                    <i class="fa fa-trash" />
                            </button>
                            <div
                                t-attf-id="track-application-speakers-row-div-#{speaker_count}"
                                class="row"
                            >
                                <div class="form-group col-xl-4">
                                    <label
                                        t-attf-for="speaker_firstname[#{speaker_count}]"
                                        class="col-form-label"
                                    >First Name</label>
                                    <input
                                        t-attf-name="speaker_firstname[#{speaker_count}]"
                                        placeholder="First Name"
                                        t-att-value="speaker.firstname"
                                        class="form-control"
                                        type="text"
                                        t-att-required="None if not editable else 'required'"
                                        t-att-disabled="None if editable else 'disabled'"
                                    />
                                </div>
                                <div class="form-group col-xl-4">
                                    <label
                                        t-attf-for="speaker_lastname[#{speaker_count}]"
                                        class="col-form-label"
                                    >Last Name</label>
                                    <input
                                        t-attf-name="speaker_lastname[#{speaker_count}]"
                                        placeholder="Last Name"
                                        t-att-value="speaker.lastname"
                                        class="form-control"
                                        type="text"
                                        t-att-required="None if not editable else 'required'"
                                        t-att-disabled="None if editable else 'disabled'"
                                    />
                                </div>
                                <div class="form-group col-xl-4">
                                    <label
                                        t-attf-for="speaker_email[#{speaker_count}]"
                                        class="col-form-label"
                                    >Email</label>
                                    <input
                                        t-attf-name="speaker_email[#{speaker_count}]"
                                        placeholder="Email"
                                        t-att-value="speaker.email"
                                        class="form-control"
                                        type="email"
                                        t-att-required="None if not editable else 'required'"
                                        t-att-disabled="None if editable else 'disabled'"
                                    />
                                </div>
                                <div class="form-group col-xl-4">
                                    <label
                                        t-attf-for="speaker_phone[#{speaker_count}]"
                                        class="col-form-label label-optional"
                                    >Phone</label>
                                    <input
                                        t-attf-name="speaker_phone[#{speaker_count}]"
                                        placeholder="Phone"
                                        t-att-value="speaker.phone"
                                        class="form-control"
                                        type="tel"
                                        t-att-disabled="None if editable else 'disabled'"
                                    />
                                </div>
                                <div class="form-group col-xl-4">
                                    <label
                                        t-attf-for="speaker_organization[#{speaker_count}]"
                                        class="col-form-label"
                                    >Organization</label>
                                    <input
                                        t-attf-name="speaker_organization[#{speaker_count}]"
                                        placeholder="Organization"
                                        t-att-value="speaker.parent_id.name"
                                        class="form-control"
                                        type="text"
                                        t-att-required="None if not editable else 'required'"
                                        t-att-disabled="None if editable else 'disabled'"
                                    />
                                </div>
                                <div class="form-group col-xl-4">
                                    <label
                                        t-attf-for="speaker_title[#{speaker_count}]"
                                        class="col-form-label"
                                    >Title</label>
                                    <input
                                        t-attf-name="speaker_title[#{speaker_count}]"
                                        placeholder="Title"
                                        t-att-value="speaker.function"
                                        class="form-control"
                                        type="text"
                                        t-att-required="None if not editable else 'required'"
                                        t-att-disabled="None if editable else 'disabled'"
                                    />
                                </div>
                            </div>
                        </div>
                    </t>
                    <!-- If no existing speakers, add a single empty speaker -->
                    <t t-if="speaker_count == 0">
                        <t t-set="speaker_count" t-value="speaker_count + 1" />
                        <div
                            class="track-application-speakers-div-row-container"
                            t-att-id="speaker_count"
                        >
                            <span class="text-muted presenter-span">Presenter #<t
                                    t-esc="speaker_count"
                                /></span>
                            <button
                                type="button"
                                class="btn btn-default btn-remove-speaker"
                                disabled="disabled"
                                data-toggle="tooltip"
                                data-placement="right"
                                title="Remove speaker"
                            >
                                <i class="fa fa-trash" />
                            </button>
                            <div
                                t-attf-id="track-application-speakers-row-div-#{speaker_count}"
                                class="row"
                            >
                                <div class="form-group col-xl-4">
                                    <label
                                        t-attf-for="speaker_firstname[#{speaker_count}]"
                                        class="col-form-label"
                                    >First Name</label>
                                    <input
                                        t-attf-name="speaker_firstname[#{speaker_count}]"
                                        placeholder="First Name"
                                        class="form-control"
                                        type="text"
                                        t-att-value="partner.firstname if partner else False"
                                        t-att-required="None if not editable else 'required'"
                                        t-att-disabled="None if editable else 'disabled'"
                                    />
                                </div>
                                <div class="form-group col-xl-4">
                                    <label
                                        t-attf-for="speaker_lastname[#{speaker_count}]"
                                        class="col-form-label"
                                    >Last Name</label>
                                    <input
                                        t-attf-name="speaker_lastname[#{speaker_count}]"
                                        placeholder="Last Name"
                                        class="form-control"
                                        type="text"
                                        t-att-value="partner.lastname if partner else False"
                                        t-att-required="None if not editable else 'required'"
                                        t-att-disabled="None if editable else 'disabled'"
                                    />
                                </div>
                                <div class="form-group col-xl-4">
                                    <label
                                        t-attf-for="speaker_email[#{speaker_count}]"
                                        class="col-form-label"
                                    >Email</label>
                                    <input
                                        t-attf-name="speaker_email[#{speaker_count}]"
                                        placeholder="Email"
                                        class="form-control"
                                        type="email"
                                        t-att-value="partner.email if partner else False"
                                        t-att-required="None if not editable else 'required'"
                                        t-att-disabled="None if editable else 'disabled'"
                                    />
                                </div>
                                <div class="form-group col-xl-4">
                                    <label
                                        t-attf-for="speaker_phone[#{speaker_count}]"
                                        class="col-form-label label-optional"
                                    >Phone</label>
                                    <input
                                        t-attf-name="speaker_phone[#{speaker_count}]"
                                        placeholder="Phone"
                                        class="form-control"
                                        type="tel"
                                        t-att-value="partner.phone if partner else False"
                                        t-att-disabled="None if editable else 'disabled'"
                                    />
                                </div>
                                <div class="form-group col-xl-4">
                                    <label
                                        t-attf-for="speaker_organization[#{speaker_count}]"
                                        class="col-form-label"
                                    >Organization</label>
                                    <input
                                        t-attf-name="speaker_organization[#{speaker_count}]"
                                        placeholder="Organization"
                                        class="form-control"
                                        type="text"
                                        t-att-value="partner.parent_id.name if partner else False"
                                        t-att-required="None if not editable else 'required'"
                                        t-att-disabled="None if editable else 'disabled'"
                                    />
                                </div>
                                <div class="form-group col-xl-4">
                                    <label
                                        t-attf-for="speaker_title[#{speaker_count}]"
                                        class="col-form-label"
                                    >Title</label>
                                    <input
                                        t-attf-name="speaker_title[#{speaker_count}]"
                                        placeholder="Title"
                                        class="form-control"
                                        type="text"
                                        t-att-value="partner.function if partner else False"
                                        t-att-required="None if not editable else 'required'"
                                        t-att-disabled="None if editable else 'disabled'"
                                    />
                                </div>
                            </div>
                        </div>
                    </t>
                </div>
                <input
                    id="track-application-speaker-input-index"
                    name="speakers_input_index"
                    t-att-value="speaker_count"
                    class="d-none"
                    type="hidden"
                />
                <div class="form-group">
                    <button
                        type="button"
                        class="btn btn-primary"
                        t-att-disabled="None if editable else 'disabled'"
                        id="add_speaker"
                    >Add Presenter <i class="fa fa-user" />
                    </button>
                </div>
            </div>
        </div>
    </template>
    <!-- WEBINAR -->
    <template id="event_track_application_webinar">
        <div
            t-attf-class="panel panel-warning #{'d-none' if not track.type.webinar else ''}"
            id="track-application-webinar-div"
        >
            <div class="panel-heading">
                <h3 class="panel-title">Webinar Details</h3>
            </div>
            <div class="panel-body">
                <p
                    id="track-application-webinar-alert-info"
                    class="alert alert-info alert-dismissable"
                >
                    <a
                        href="#"
                        class="close"
                        data-dismiss="alert"
                        aria-label="close"
                    >×</a>
                    <span
                    >You can fill this if you want to hold a webinar before the actual event.</span>
                </p>
                <div class="row" id="track-application-webinar-row-div">
                    <input
                        type="hidden"
                        name="is_webinar"
                        id="is_webinar"
                        t-attf-value="#{'true' if track.type.webinar else 'false'}"
                    />
                    <div class="form-group col-xl-12">
                        <div class="form-check">
                            <input
                                type="checkbox"
                                class="form-check-input"
                                name="webinar"
                                data-toggle="webinar_info"
                                t-att-checked="None if not track.webinar else 'checked'"
                                t-att-disabled="None if editable and track.type.webinar else 'disabled'"
                            />
                            <label
                                for="webinar"
                                class="form-check-label label-optional"
                            >I would like to hold a webinar</label>
                        </div>
                    </div>
                    <div class="form-group col-xl-12">
                        <label
                            for="webinar_info"
                            class="col-form-label label-optional"
                        >Webinar details</label>
                        <textarea
                            type="text"
                            class="form-control o_wysiwyg_loader"
                            id="webinar_info"
                            name="webinar_info"
                            placeholder="Webinar info..."
                            rows="6"
                            t-att-disabled="None if editable and track.type.webinar and track.webinar else 'disabled'"
                        ><t t-esc="track.webinar_info" /></textarea>
                    </div>
                </div>
            </div>
        </div>
    </template>

    <!-- WORKSHOP -->
    <template id="event_track_application_workshop">
        <div
            t-attf-class="panel panel-warning #{'d-none' if not track.type.workshop else ''}"
            id="track-application-workshop-div"
        >
            <div class="panel-heading">
                <h3 class="panel-title">Session</h3>
            </div>
            <div class="panel-body">
                <p
                    id="track-application-workshop-alert-info"
                    class="alert alert-info alert-dismissable"
                ><a href="#" class="close" data-dismiss="alert" aria-label="close">×</a>
                    Session information and invoicing address.<br />
                    The contract will be sent for the signee.
                </p>
                <div class="row" id="track-application-workshop-row-div">
                    <input
                        type="hidden"
                        name="is_workshop"
                        id="is_workshop"
                        t-attf-value="#{'true' if track.type.workshop else 'false'}"
                    />
                    <div class="form-group col-xl-6">
                        <label
                            class="col-form-label"
                            for="workshop_participants"
                        >Min Participants</label>
                        <input
                            type="number"
                            name="workshop_min_participants"
                            class="form-control"
                            placeholder="0"
                            t-att-value="track.workshop_min_participants"
                            t-att-required="None if not track.type.workshop or not editable else 'required'"
                            t-att-disabled="None if editable and track.type.workshop else 'disabled'"
                            t-att-required-disabled="None if editable and track.type.workshop else 'disabled'"
                        />
                    </div>
                    <div class="form-group col-xl-6">
                        <label
                            class="col-form-label"
                            for="workshop_participants"
                        >Max Participants</label>
                        <input
                            type="number"
                            name="workshop_participants"
                            class="form-control"
                            placeholder="0"
                            t-att-value="track.workshop_participants"
                            t-att-required="None if not track.type.workshop or not editable else 'required'"
                            t-att-disabled="None if editable and track.type.workshop else 'disabled'"
                            t-att-required-disabled="None if editable and track.type.workshop else 'disabled'"
                        />
                    </div>
                    <div class="form-group col-xl-6">
                        <label
                            class="col-form-label"
                            for="workshop_fee"
                        >Participation Fee</label>
                        <input
                            type="number"
                            name="workshop_fee"
                            class="form-control"
                            placeholder="0"
                            t-att-value="track.workshop_fee"
                            t-att-required="None if not track.type.workshop or not editable else 'required'"
                            t-att-disabled="None if editable and track.type.workshop else 'disabled'"
                            t-att-required-disabled="None if editable and track.type.workshop else 'disabled'"
                        />
                    </div>
                    <div class="form-group col-xl-12">
                        <label
                            for="workshop_goals"
                            class="col-form-label"
                        >Session Goals</label>
                        <textarea
                            type="text"
                            name="workshop_goals"
                            placeholder="Session Goals..."
                            class="form-control o_wysiwyg_loader"
                            t-att-required="None if not track.type.workshop or not editable else 'required'"
                            t-att-disabled="None if editable and track.type.workshop else 'disabled'"
                            t-att-required-disabled="None if editable and track.type.workshop else 'disabled'"
                            rows="6"
                        ><t t-esc="track.workshop_goals" /></textarea>
                        <div class="invalid-feedback">Please enter session goals.</div>
                    </div>
                    <div class="form-group col-xl-12">
                        <label
                            for="workshop_schedule"
                            class="col-form-label"
                        >Session Program and Schedule</label>
                        <textarea
                            type="text"
                            name="workshop_schedule"
                            placeholder="Session Schedule..."
                            class="form-control o_wysiwyg_loader"
                            t-att-required="None if not track.type.workshop or not editable else 'required'"
                            t-att-disabled="None if editable and track.type.workshop else 'disabled'"
                            t-att-required-disabled="None if editable and track.type.workshop else 'disabled'"
                            rows="6"
                        ><t t-esc="track.workshop_schedule" /></textarea>
                        <div
                            class="invalid-feedback"
                        >Please enter session program and schedule.</div>
                    </div>
                </div>
                <t t-if="track.type.workshop_contract">
                <div
                        t-att-class="'d-none' if not track.type.workshop_contract else ''"
                        id="track-application-workshop-contract-div"
                    >
                <h5>Contract Signee</h5>
                <div id="track-application-organization-signee-row-div" class="row">
                    <input
                                type="hidden"
                                name="is_workshop_contract"
                                id="is_workshop_contract"
                                t-attf-value="#{'true' if track.type.workshop_contract else 'false'}"
                            />
                    <div class="form-group col-xl-4">
                        <label
                                    for="signee_firstname"
                                    class="col-form-label"
                                >First Name</label>
                        <input
                                    name="signee_firstname"
                                    placeholder="First Name"
                                    t-att-value="track.organizer_contact.firstname"
                                    t-att-required="None if not track.type.workshop or not editable else 'required'"
                                    t-att-disabled="None if editable and track.type.workshop else 'disabled'"
                                    t-att-required-disabled="None if editable and track.type.workshop else 'disabled'"
                                    class="form-control"
                                    type="text"
                                />
                    </div>
                    <div class="form-group col-xl-4">
                        <label
                                    for="signee_lastname"
                                    class="col-form-label"
                                >Last Name</label>
                        <input
                                    name="signee_lastname"
                                    placeholder="Last Name"
                                    t-att-value="track.organizer_contact.lastname"
                                    class="form-control"
                                    type="text"
                                    t-att-required="None if not track.type.workshop or not editable else 'required'"
                                    t-att-disabled="None if editable and track.type.workshop else 'disabled'"
                                    t-att-required-disabled="None if editable and track.type.workshop else 'disabled'"
                                />
                    </div>
                    <div class="form-group col-xl-4">
                        <label for="signee_email" class="col-form-label">Email</label>
                        <input
                                    name="signee_email"
                                    placeholder="Email"
                                    t-att-value="track.organizer_contact.email"
                                    class="form-control"
                                    type="email"
                                    t-att-required="None if not track.type.workshop or not editable else 'required'"
                                    t-att-disabled="None if editable and track.type.workshop else 'disabled'"
                                    t-att-required-disabled="None if editable and track.type.workshop else 'disabled'"
                                />
                    </div>
                    <div class="form-group col-xl-4">
                        <label
                                    for="signee_phone"
                                    class="col-form-label label-optional"
                                >Phone</label>
                        <input
                                    name="signee_phone"
                                    placeholder="Phone"
                                    t-att-value="track.organizer_contact.phone"
                                    class="form-control"
                                    type="tel"
                                    t-att-disabled="None if editable and track.type.workshop else 'disabled'"
                                />
                    </div>
                    <div class="form-group col-xl-4">
                        <label
                                    for="signee_organization"
                                    class="col-form-label label-optional"
                                >Organization</label>
                        <input
                                    name="signee_organization"
                                    placeholder="Organization"
                                    t-att-value="track.organizer_contact.parent_id.name"
                                    class="form-control"
                                    type="text"
                                    t-att-disabled="None if editable and track.type.workshop else 'disabled'"
                                />
                    </div>
                    <div class="form-group col-xl-4">
                        <label for="signee_title" class="col-form-label">Title</label>
                        <input
                                    name="signee_title"
                                    placeholder="Title"
                                    t-att-value="track.organizer_contact.function"
                                    class="form-control"
                                    type="text"
                                    t-att-required="None if not track.type.workshop or not editable else 'required'"
                                    t-att-disabled="None if editable and track.type.workshop else 'disabled'"
                                    t-att-required-disabled="None if editable and track.type.workshop else 'disabled'"
                                />
                    </div>
                </div>
                <h5>Organization invoice address</h5>
                <div class="row" id="track-application-organization-invoice-row-div">
                    <div class="form-group col-xl-4">
                        <label
                                    for="organizer_organization"
                                    class="col-form-label"
                                >Organization</label>
                        <input
                                    name="organizer_organization"
                                    placeholder="Organization"
                                    t-att-value="track.organizer.name"
                                    class="form-control"
                                    type="text"
                                    t-att-required="None if not track.type.workshop or not editable else 'required'"
                                    t-att-disabled="None if editable and track.type.workshop else 'disabled'"
                                    t-att-required-disabled="None if editable and track.type.workshop else 'disabled'"
                                />
                    </div>
                    <div class="form-group col-xl-4">
                        <label
                                    for="organizer_street"
                                    class="col-form-label"
                                >Street</label>
                        <input
                                    name="organizer_street"
                                    placeholder="Street"
                                    t-att-value="track.organizer.street"
                                    class="form-control"
                                    type="text"
                                    t-att-required="None if not track.type.workshop or not editable else 'required'"
                                    t-att-disabled="None if editable and track.type.workshop else 'disabled'"
                                    t-att-required-disabled="None if editable and track.type.workshop else 'disabled'"
                                />
                    </div>
                    <div class="form-group col-xl-4">
                        <label for="organizer_zip" class="col-form-label">Zip</label>
                        <input
                                    name="organizer_zip"
                                    placeholder="Zip"
                                    t-att-value="track.organizer.zip"
                                    class="form-control"
                                    type="text"
                                    t-att-required="None if not track.type.workshop or not editable else 'required'"
                                    t-att-disabled="None if editable and track.type.workshop else 'disabled'"
                                    t-att-required-disabled="None if editable and track.type.workshop else 'disabled'"
                                />
                    </div>
                    <div class="form-group col-xl-4">
                        <label for="organizer_city" class="col-form-label">City</label>
                        <input
                                    name="organizer_city"
                                    placeholder="City"
                                    t-att-value="track.organizer.city"
                                    class="form-control"
                                    type="text"
                                    t-att-required="None if not track.type.workshop or not editable else 'required'"
                                    t-att-disabled="None if editable and track.type.workshop else 'disabled'"
                                    t-att-required-disabled="None if editable and track.type.workshop else 'disabled'"
                                />
                    </div>
                    <div class="form-group col-xl-4">
                        <t
                                    t-set="operators"
                                    t-value="request.env['res.partner.operator.einvoice'].sudo().search([])"
                                />
                        <label
                                    class="col-form-label"
                                    for="einvoice_operator_id"
                                >eInvoice Operator</label>
                        <select
                                    name="einvoice_operator_id"
                                    id="einvoice_operator_id"
                                    placeholder="Einvoice operator..."
                                    class="form-control"
                                    autocomplete="off"
                                    t-att-required="None if not track.type.workshop or not editable else 'required'"
                                    t-att-disabled="None if editable and track.type.workshop else 'disabled'"
                                    t-att-required-disabled="None if editable and track.type.workshop else 'disabled'"
                                >
                                <option value="">Select...</option>
                                <t t-foreach="operators" t-as="o">
                                    <option
                                            t-att-value="o.id"
                                            t-att-selected="o.id == track.organizer.einvoice_operator_id.id"
                                        ><t t-esc="o.name" /></option>
                                </t>
                            </select>
                    </div>
                    <div class="form-group col-xl-4">
                        <label class="col-form-label" for="edicode">Edicode</label>
                        <input
                                    name="edicode"
                                    placeholder="Edicode"
                                    t-att-value="track.organizer.edicode"
                                    class="form-control"
                                    type="text"
                                    t-att-required="None if not track.type.workshop or not editable else 'required'"
                                    t-att-disabled="None if editable and track.type.workshop else 'disabled'"
                                    t-att-required-disabled="None if editable and track.type.workshop else 'disabled'"
                                />
                    </div>
                    <div class="form-group col-xl-4">
                        <label
                                    for="organizer_reference"
                                    class="col-form-label"
                                >Your Reference</label>
                        <input
                                    name="organizer_reference"
                                    placeholder="Your Reference"
                                    t-att-value="track.organizer.ref"
                                    class="form-control"
                                    type="text"
                                    t-att-required="None if not track.type.workshop or not editable else 'required'"
                                    t-att-disabled="None if editable and track.type.workshop else 'disabled'"
                                    t-att-required-disabled="None if editable and track.type.workshop else 'disabled'"
                                />
                    </div>
                </div>
                </div>
                </t>
            </div>
        </div>
    </template>

    <!-- EXTRA INFO -->
    <template id="event_track_application_extra_info">
        <div id="event-track-application-extra-info" class="panel panel-warning">
            <div class="panel-heading">
                <h3 class="panel-title">Additional details</h3>
            </div>
            <div class="panel-body" id="track-application-extra-div">
                <p
                    id="track-application-extra-alert-info"
                    class="alert alert-info alert-dismissable"
                >
                    <a
                        href="#"
                        class="close"
                        data-dismiss="alert"
                        aria-label="close"
                    >×</a>
                    <span
                    >Please enter any special arrangements you wish for your presentation in the additional details section.</span>
                    <br />
                    <span
                    >For example, any wishes for presentation technology, premises, furniture, presentation time, etc.</span>
                </p>
                <div class="row" id="track-application-extra-info-row-div">
                    <div class="form-group col-xl-12">
                        <label
                            for="extra_info"
                            class="col-form-label label-optional"
                        >Extra Info</label>
                        <textarea
                            type="text"
                            name="extra_info"
                            placeholder="Extra Info..."
                            class="form-control o_wysiwyg_loader"
                            t-att-disabled="None if editable else 'disabled'"
                            rows="6"
                        ><t t-esc="track.extra_info" /></textarea>
                    </div>
                </div>
            </div>
        </div>
    </template>

    <!-- REVIEW -->
    <template id="event_track_application_review">
        <div id="event-track-application-review" class="panel panel-warning">
            <div class="panel-heading">
                <h3 class="panel-title">Review</h3>
            </div>
            <div class="panel-body" id="track-application-review-div">
                <div class="row" id="track-application-review-row-div">
                    <div class="form group col-xl-12">
                        <label for="rating" class="col-form-label">Rating</label>
                        <select
                            name="rating"
                            id="rating"
                            class="form-control"
                            required="required"
                        >
                            <option value="">Select...</option>
                            <t
                                t-foreach="event.rating_grade_ids or []"
                                t-as="rating_grade"
                            >
                                <option
                                    t-att-value="rating_grade.id"
                                    t-att-selected="rating_grade.id == existing_rating.grade_id.id"
                                >
                                    <t t-esc="rating_grade.name" />
                                </option>
                            </t>
                        </select>
                        <div
                            class="invalid-feedback"
                        >Please select a rating grade.</div>
                    </div>
                    <div class="form-group col-xl-12">
                        <label
                            for="rating_comment"
                            class="col-form-label label-optional"
                        >Verbal Rating</label>
                        <textarea
                            type="text"
                            id="rating_comment"
                            name="rating_comment"
                            placeholder="Verbal Rating..."
                            class="form-control o_wysiwyg_loader"
                            rows="6"
                        ><t t-esc="existing_rating.comment" /></textarea>
                    </div>
                </div>
            </div>
        </div>
    </template>
</odoo>
