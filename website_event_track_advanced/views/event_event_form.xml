<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <act_window id="act_event_track_assign"
                name="Assign reviewers"
                src_model="event.event"
                res_model="event.track.assign.wizard"
                view_mode="form"
                target="new"
                key2="client_action_multi"/>

    <record id="view_event_form" model="ir.ui.view">
        <field name="name">view_event_form_call_for_papers</field>
        <field name="model">event.event</field>
        <field name="inherit_id" ref="event.view_event_form"/>
        <field name="arch" type="xml">
            <button name="button_cancel" position="after">
                <button name="%(act_event_track_assign)d"
                        type="action"
                        string="Assign reviewers"
                />
            </button>

            <group name="main_field_group" position="after">
                <group>
                    <field name="allowed_track_tag_ids" widget="many2many_tags"/>
                </group>
            </group>

            <notebook position="inside">
                <page string="Tracks">
                    <field name="track_ids"
                           context="{'tree_view_ref': 'website_event_track_advanced.view_event_track_advanced_tree'}"
                    />
                </page>
            </notebook>

        </field>
    </record>
</odoo>