<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- New the proposal layout -->
    <template name="Event track proposal" id="event_track_proposal">
        <t t-call="website_event.layout">

            <div class="oe_structure"/>
            <div class="container">
                <section>
                    <h1 class="text-center mb0">Call for Papers</h1>
                    <h2 class="text-center text-muted mb32" t-esc="event.name"></h2>
                </section>
                <section id="forms" t-if="not event.show_track_proposal">
                    <h1>Proposals are closed!</h1>
                    <p>
                        This event does not accept proposals.
                    </p>
                </section>

                <t t-if="event.show_track_proposal">
                    <section class="row" id="track-application-section">
                        <div class="col-md-12">
                            <div class="oe_structure">
                                <section>
                                    <h3 class="page-header mt16">
                                        Application
                                    </h3>
                                    <div id="track-application-section-alert" class="alert">
                                        <p>We will accept a broad range of
                                        presentations, from reports on academic and
                                        commercial projects to tutorials and case
                                        studies. As long as the presentation is
                                        interesting and potentially useful to the
                                        audience, it will be considered for
                                        inclusion in the programme.
                                        </p>
                                    </div>

                                    <p class="alert text-primary">Required fields are marked with *</p>
                                </section>

                                <section id="forms">
                                    <form id="track-application-form"
                                          class="form-horizontal mt32 js_website_submit_form"
                                          t-attf-action="/event/#{event.id}/track_proposal/post"
                                          method="post"
                                          enctype="multipart/form-data">

                                        <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>

                                        <t t-call="website_event_track_advanced.event_track_application"/>


                                        <p class="alert alert-info alert-dismissable text-center">
                                            <a href="#" class="close" data-dismiss="alert" aria-label="close">&#215;</a>
                                            By clicking <strong>"Save as draft"</strong> the proposition will be saved for later editing.
                                        </p>

                                        <p class="alert alert-info alert-dismissable text-center">
                                            <a href="#" class="close" data-dismiss="alert" aria-label="close">&#215;</a>
                                            By clicking <strong>"Save and send"</strong> the proposition will be saved and sent for review.
                                        </p>

                                        <div class="form-group">
                                            <input type="submit"
                                                   name="track-save"
                                                   id="application-submit-button"
                                                   class="btn btn-primary col-xs-10 col-xs-offset-1"
                                                   value="Save as draft" />

                                            <input t-if="track.state in ['draft', ''] or not track.state"
                                                   type="submit"
                                                   name="track-confirm"
                                                   id="application-submit-button"
                                                   class="btn btn-primary col-xs-10 col-xs-offset-1 mt8"
                                                   value="Save and send" />
                                        </div>
                                    </form>
                                </section>
                            </div>
                        </div>
                    </section>
                </t>

            </div>
            <div class="oe_structure"/>
        </t>

    </template>

</odoo>
