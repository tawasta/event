<?xml version="1.0" encoding="utf-8" ?>
<odoo noupdate="0">>


        <record
        id="mail_template_event_registration_survey_recap"
        model="mail.template"
    >
            <field name="name">Event Registration: Survey Recap</field>
            <field name="model_id" ref="event.model_event_registration" />
            <field
            name="subject"
        >Your survey answers for {{ object.event_id.name }}</field>
            <field name="email_from">{{ (object.event_id.organizer_id.email_formatted
                or object.company_id.email_formatted
                or user.email_formatted) }}</field>
            <field name="email_to">{{ object.email }}</field>
            <field
            name="body_html"
        ><![CDATA[
<div style="margin: 0px; padding: 0px; font-size: 13px;">
    <p>Dear {{ object.name or 'Participant' }},</p>
    <p>
        Thank you for registering for
        <strong>{{ object.event_id.name }}</strong>.
    </p>
    <p>
        All your survey responses have been processed.
        Below is a summary of the surveys completed:
    </p>
    <ul>
        {% for answer in object.survey_answer_ids %}
        <li>{{ answer.survey_id.title }}</li>
        {% endfor %}
    </ul>
    <p>Kind regards,<br/>
        {{ object.event_id.organizer_id.name or object.company_id.name }}
    </p>
</div>
]]></field>
            <field name="auto_delete" eval="True" />
            <field name="lang">{{ object.partner_id.lang }}</field>
        </record>

</odoo>
